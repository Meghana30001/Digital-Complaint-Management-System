/**
 * Production API URL — required for your deployed site (Vercel, Netlify, etc.)
 *
 * 1. Deploy the backend (server/ folder) to Render, Railway, etc.
 * 2. Paste your live API base below (must end with /api)
 * 3. Push to GitHub so Vercel redeploys
 *
 * Example: https://dcms-api.onrender.com/api
 */
window.DCMS_PROD_API = 'https://digital-complaint-management-system.onrender.com/api';
